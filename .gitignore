node_modules

# Output
.output
.vercel
.netlify
.wrangler
# Nicht mehr wurzel-verankert: seit dem Umzug (PR 3) liegt die App in apps/docs
# und erzeugt ihre Build-Artefakte dort.
.svelte-kit/
build/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Test-Reports (Build-Artefakt, wird pro Run neu erzeugt) — vitest läuft in apps/docs.
reports/

# Lokales Claude-Code-Tooling (z. B. Preview-Dev-Server-Config)
/.claude/

# Editor-Cruft
.obsidian/

# Playwright-Artefakte
test-results/
playwright-report/

# Icon-Auslieferung: reiner Spiegel von packages/icons/svg (npm run sync:icons,
# läuft in prepare/predev/prebuild). Quelle ist das Paket @zeit/icons.
/apps/docs/static/downloads/icons/

# Token-Auslieferung: reiner Spiegel von packages/tokens/vendor (npm run sync:zds,
# läuft in prepare/predev/prebuild). Quelle ist das Paket @zeit/tokens.
/apps/docs/static/styles-zds.css

# Paket-Build-Artefakte (npm run tokens:build)
/packages/*/dist/

# Prod-Drift-Bericht (tooling/check-prod-drift.mjs --json/--summary, CI-Artefakt)
/prod-drift.json
/prod-drift.md
