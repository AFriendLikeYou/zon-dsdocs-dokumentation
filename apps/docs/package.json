{
	"name": "docs",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"predev": "node ../../tooling/sync-icons.mjs && node ../../tooling/sync-zds.mjs",
		"prebuild": "node ../../tooling/sync-icons.mjs && node ../../tooling/sync-zds.mjs",
		"sync": "svelte-kit sync",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"test:unit": "vitest",
		"test": "vitest run",
		"test:e2e": "playwright test"
	}
}
